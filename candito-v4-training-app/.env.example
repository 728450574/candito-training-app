# CloudBase 云存储配置（构建时通过 Vite 环境变量注入）
# 复制本文件为 .env.local 并填入真实值；或在部署平台配置同名环境变量。
# 未配置时应用默认使用本地存储，用户无法切换到云端同步。

# 环境ID
VITE_CLOUDBASE_ENV_ID=

# 环境所在地域
VITE_CLOUDBASE_REGION=ap-shanghai

# 匿名访问令牌
VITE_CLOUDBASE_ACCESS_KEY=
